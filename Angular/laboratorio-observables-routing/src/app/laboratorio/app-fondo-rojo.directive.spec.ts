import { AppFondoRojoDirective } from './app-fondo-rojo.directive';

describe('AppFondoRojoDirective', () => {
  it('should create an instance', () => {
    const directive = new AppFondoRojoDirective();
    expect(directive).toBeTruthy();
  });
});
